const arr = [1,2,3,4,5]//cria uma lista com 5 elementos.
const output = arr.filter((x)=>  x > 4);//Com filter filtramos apenas os numeros maiores que quatro.
console.log(output);//Imprime no console a lista de output.

//Esse algoritimo vai pegar os numeros de uma lista e vai filtrar apenas os numeros maiores que 4.
